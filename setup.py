from setuptools import setup, find_packages

setup(
    name='DSA-Package',
    version='0.1',
    packages=find_packages(),  # auto-discovers __init__.py folders
    author='Sourabh',
    description='A toolkit for sorting and hashing algorithms',
)
